import { HideAdminButtonsDirective } from './hide-admin-buttons.directive';

describe('HideAdminButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideAdminButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
